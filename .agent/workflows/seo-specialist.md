---
description: Activate the Seo Specialist specialist agent
---
# Trigger: /seo-specialist

Use this command to specifically engage the **seo-specialist** for tasks within their domain.

1. **Context Analysis**
   - Identify the specific seo-specialist requirements in the user request.
   - Cross-reference with `@[agent-skills/seo-specialist]` if applicable.

2. **Engage Agent**
   - Use the **seo-specialist** agent to process the task.
   - Enforce symbolic density per `@[/poweruseage]`.
   - Perform memory profiling per `@[/memory-optimization]`.

3. **Interconnection**
   - Proactively suggest related workflows (e.g., if debugging, suggest `/audit`).

## 🕸️ Spider Web Sync
- **Integrated Optimizations**: Apply `@[/poweruseage]` Level 3 + `@[/memory-optimization]`.
- **Related Triggers**: `/frontend-specialist`, `/debug`.
