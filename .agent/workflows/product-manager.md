---
description: Activate the Product Manager specialist agent
---
# Trigger: /product-manager

Use this command to specifically engage the **product-manager** for tasks within their domain.

1. **Context Analysis**
   - Identify the specific product-manager requirements in the user request.
   - Cross-reference with `@[agent-skills/product-manager]` if applicable.

2. **Engage Agent**
   - Use the **product-manager** agent to process the task.
   - Enforce symbolic density per `@[/poweruseage]`.
   - Perform memory profiling per `@[/memory-optimization]`.

3. **Interconnection**
   - Proactively suggest related workflows (e.g., if debugging, suggest `/audit`).

## 🕸️ Spider Web Sync
- **Integrated Optimizations**: Apply `@[/poweruseage]` Level 3 + `@[/memory-optimization]`.
- **Related Triggers**: `/frontend-specialist`, `/debug`.
