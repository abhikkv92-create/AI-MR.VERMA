---
description: Activate the Devops Engineer specialist agent
---
# Trigger: /devops-engineer

Use this command to specifically engage the **devops-engineer** for tasks within their domain.

1. **Context Analysis**
   - Identify the specific devops-engineer requirements in the user request.
   - Cross-reference with `@[agent-skills/devops-engineer]` if applicable.

2. **Engage Agent**
   - Use the **devops-engineer** agent to process the task.
   - Enforce symbolic density per `@[/poweruseage]`.
   - Perform memory profiling per `@[/memory-optimization]`.

3. **Interconnection**
   - Proactively suggest related workflows (e.g., if debugging, suggest `/audit`).

## 🕸️ Spider Web Sync
- **Integrated Optimizations**: Apply `@[/poweruseage]` Level 3 + `@[/memory-optimization]`.
- **Related Triggers**: `/frontend-specialist`, `/debug`.
