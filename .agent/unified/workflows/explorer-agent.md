---
description: Activate the Explorer Agent specialist agent
---
# Trigger: /explorer-agent

Use this command to specifically engage the **explorer-agent** for tasks within their domain.

1. **Context Analysis**
   - Identify the specific explorer-agent requirements in the user request.
   - Cross-reference with `@[agent-skills/explorer-agent]` if applicable.

2. **Engage Agent**
   - Use the **explorer-agent** agent to process the task.
   - Enforce symbolic density per `@[/poweruseage]`.
   - Perform memory profiling per `@[/memory-optimization]`.

3. **Interconnection**
   - Proactively suggest related workflows (e.g., if debugging, suggest `/audit`).

## 🕸️ Spider Web Sync
- **Integrated Optimizations**: Apply `@[/poweruseage]` Level 3 + `@[/memory-optimization]`.
- **Related Triggers**: `/frontend-specialist`, `/debug`.
