---
description: AI-powered feature integration with RAG and LLM chains. Adds AI capabilities to applications.
---

# /ai-feature - AI Feature Integration

$ARGUMENTS: [feature_description]

## 🤖 Applied Agents: `ai-researcher`, `backend-specialist`, `frontend-specialist`

This workflow integrates advanced AI capabilities (RAG, LLM chains) into existing applications.

## 🛠️ Step-by-Step Execution

1.  **Model & Prompt Strategy**
    - Agent: `ai-researcher`
    - Action: Use `ai-sdk` and `gemini-token-optimization`.
    - Deliverable: Optimized system prompt and model selection (Gemini vs Claude).

2.  **RAG Pipeline Architecture**
    - Agent: `ai-researcher` + `backend-specialist`
    - Action: Deploy `ai-rag-pipeline` skill patterns.
    - Architecture: Vector DB selection + Embedding strategy.

3.  **UI/UX Implementation**
    - Agent: `frontend-specialist`
    - Action: Implement `agent-ui` components.
    - Focus: Streaming responses, loading states, optimistic updates.

4.  **Integration Testing**
    - Agent: `backend-specialist`
    - Action: Verify API cost and latency.

---

## 🚦 Output Format

Produces a **Feature Integration Plan** and code scaffolding for the AI module.

## 🕸️ Spider Web Sync
- **Integrated Optimizations**: Apply `@[/poweruseage]` Level 3 + `@[/memory-optimization]`.
- **Related Triggers**: `/frontend-specialist`, `/backend-specialist`, `/test-engineer`, `/deploy`, `/plan`.
