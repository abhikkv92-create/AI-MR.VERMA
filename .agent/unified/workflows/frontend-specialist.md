---
description: Activate the Frontend Specialist specialist agent
---
# Trigger: /frontend-specialist

Use this command to specifically engage the **frontend-specialist** for tasks within their domain.

1. **Context Analysis**
   - Identify the specific frontend-specialist requirements in the user request.
   - Cross-reference with `@[agent-skills/frontend-specialist]` if applicable.

2. **Engage Agent**
   - Use the **frontend-specialist** agent to process the task.
   - Enforce symbolic density per `@[/poweruseage]`.
   - Perform memory profiling per `@[/memory-optimization]`.

3. **Interconnection**
   - Proactively suggest related workflows (e.g., if debugging, suggest `/audit`).

## 🕸️ Spider Web Sync
- **Integrated Optimizations**: Apply `@[/poweruseage]` Level 3 + `@[/memory-optimization]`.
- **Related Triggers**: `/frontend-specialist`, `/debug`.
