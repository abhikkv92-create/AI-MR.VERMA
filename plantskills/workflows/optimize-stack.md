---
description: Full stack performance optimization for database, backend, and frontend with Core Web Vitals targets.
---

# /optimize-stack - Full Stack Optimization

$ARGUMENTS: [codebase_path]

## 🤖 Applied Agents: `database-architect`, `backend-specialist`, `frontend-specialist`

This workflow tunes every layer of the stack for maximum performance and cost efficiency.

## 🛠️ Step-by-Step Execution

1.  **Database Layer**
    - Agent: `database-architect`
    - Action: Apply `database-optimizer` skill.
    - Check: Index usage, query performance (EXPLAIN ANALYZE), schema normalization.

2.  **Backend Efficiency**
    - Agent: `backend-specialist`
    - Action: Review `python-performance-optimization` and `token-efficiency`.
    - Focus: API latency, extensive loop reduction, caching strategy.

3.  **Frontend Core Web Vitals**
    - Agent: `frontend-specialist`
    - Action: Apply `nextjs-best-practices` and `performance-profiling`.
    - Targets: LCP < 2.5s, CLS < 0.1, INP < 200ms.

4.  **Cost Analysis**
    - Agent: `product-manager`
    - Action: Use `cost-optimization` skill to project savings.

---

## 🚦 Output Format

Produces a **Performance Optimization Report** with before/after metrics.
