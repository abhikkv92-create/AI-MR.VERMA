FROM python:3.11-slim

WORKDIR /app

# Install minimal system tools if needed (psutil might need gcc/python-dev but usually wheel exists)
# RUN apt-get update && apt-get install -y gcc python3-dev && rm -rf /var/lib/apt/lists/*

COPY requirements.collector.txt .
RUN pip install --no-cache-dir -r requirements.collector.txt

COPY interaction_collector.py .

CMD ["python", "interaction_collector.py"]
